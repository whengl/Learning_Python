# Задача на замену чисел в списке. Цель программы - заменить каждое число в списке суммой двух сопредельных чисел. Для крайнего числа используется противоположное с конца в качестве второго сопредельного.
# Если на ввод подается одно число, программа выводит это число.
# Ввод: подается список чисел в одной строке. 
# Вывод: список, состоящий из сумм сопредельных чисел.
a=[int(i) for i in input().split()]
if len(a)==1:
    print (a[0])
else:
    for j in range (0,len(a)):
        if j==0:
            print (a[1]+a[-1], end=' ')
        elif j==len(a)-1:
            print (a[0]+a[-2], end=' ')
        else:
            print (a[j-1]+a[j+1], end=' ')
