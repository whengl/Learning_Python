# Вывод повторяющихся цифр из списка. 
# Ввод: список чисел. 
# Вывод: список, состоящий только из тех чисел, которые повторяются хотя бы дважды. В выводе числа не могут повторяться
a=[int(i) for i in input().split()]
a.sort()
for j in range (0, len(a)):
    if j==len(a)-1:
        continue # Исключает проверку для последнего числа, за ним нет числа для сверки
    elif ((a[j]==a[j+1]) and ((j==0) or (a[j]!=a[j-1]))):
        print (a[j])
